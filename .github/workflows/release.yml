name: Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-24.04, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - name: Dependencies (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y build-essential cmake

      - name: Build
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=Release ${{ runner.os == 'Linux' && '-DCMAKE_CXX_COMPILER=g++-14' || '' }}
          cmake --build build --config Release

      - name: Package
        shell: bash
        run: |
          mkdir dist
          [ "${{ runner.os }}" == "Windows" ] && cp build/Release/*.exe dist/ || cp build/Turret_2 dist/
          
          # copy libs and res
          if [ "${{ runner.os }}" == "Windows" ]; then
            cp build/Release/Turret_2.exe dist/
            [ -d "libs/MSVC_dll" ] && cp libs/MSVC_dll/*.dll dist/ || true
          else
            cp build/Turret_2 dist/
            [ -d "libs/GCC_so" ] && cp libs/GCC_so/*.so* dist/ || true
          fi
          [ -d "libs/licenses" ] && cp -r licenses dist/ || true
          [ -d "res" ] && cp -r res dist/ || true

          if [ "${{ runner.os }}" == "Windows" ]; then
            7z a Turret_2_win.zip ./dist/*
          else
            tar -czvf Turret_2_linux.tar.gz -C dist .
          fi

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: Turret_2_*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
